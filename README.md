# Agentic Day 4 - Multi-Agent

Multi-agent customer support system with LangChain.

## Setup

```bash
pip install -r requirements.txt
```

Create a `.env` file with your API keys.

## Run

```bash
python app.py
```

## Structure

- `app.py` - Main application with customer support agents and tools
- `prompts/` - Agent prompt configurations
